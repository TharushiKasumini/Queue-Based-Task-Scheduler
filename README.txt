Install python 3.9 or above if not.
Install Node JS if not.

Go to Server directory and Run
pip install -r requirements.txt

Back, Go to Client directory and Run:
npm install

====
To run the server:
Run: python api.py

To run the client (web application)
Run: npm run dev